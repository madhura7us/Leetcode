class Solution:
    def numJewelsInStones(self, J, S):
        
        count = 0
        for item1 in J:
            for item2 in S:
                if item1 == item2:
                   # print("Same", item1)
                    count += 1
        return count
